apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.com/Ravikumar98/gitops-course.git
    targetRevision: main
    path: charts/cert-manager
    helm:
      valueFiles:
        - ../../overrides/cert-manager.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
